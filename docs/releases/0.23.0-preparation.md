<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Release Preparation Evidence: 0.23.0

## Audit scope

- Previous tag: `v0.22.0`
- Audited head: `439d258af87f501fc9b11f5e9d6284fd78bc60b9`
- Commit count: `143`
- Ordered log SHA-256: `3ecf087447ca5eaf26a0330955f549513d438e96fc368a27692921bb1ae76f2b`
- Subject classes: `115 docs; 26 fix; 2 test`
- Review command: `git log v0.22.0..439d258af87f501fc9b11f5e9d6284fd78bc60b9`
- Digest command: `git log --reverse --format='%H%x09%s' v0.22.0..439d258af87f501fc9b11f5e9d6284fd78bc60b9 | shasum -a 256`
- Review attestation: `complete`

Every one of the 143 commits was reviewed from its message and diff.
Review-only follow-ups were consolidated into the concern they settled; superseded intermediate implementations were reconciled to the final shipped outcome rather than reported as separate changes.

## Semantic Versioning classification

- Previous version: `0.22.0`
- Chosen version: `0.23.0`
- Release date: `2026-08-29`
- Change level: `minor`

The project remains in SemVer's `0.y.z` initial-development phase, and its established convention advances MINOR for backward-incompatible pre-1.0 changes.
This range changes released OpenCode option behavior and the public specification paths and identifiers, and it adds repository verification capabilities; PATCH would understate the release, while `1.0.0` would declare a stable public API the project has not yet promised.

## Notable-change reconciliation

The changelog key is a unique phrase in the corresponding `0.23.0` entry.

| Heading | Changelog key                                        | Commit evidence                                                             |
| ------- | ---------------------------------------------------- | --------------------------------------------------------------------------- |
| Added   | `Audited release-preparation evidence`               | containing release-preparation commit; DR-020                               |
| Added   | `Git-aware SPDX header audit`                        | `8770d25`, `8d45bb0`                                                        |
| Changed | `OpenCode now rejects an explicit maxTurns`          | `9ba640f`, superseded by `bdc56e3`                                          |
| Changed | `canonical specification source`                     | all 115 documentation commits in the audited digest; DR-017, DR-018, DR-019 |
| Changed | `tmux-play CLI mode dispatch and startup pane order` | `5f2b033`, `8dbf228`, `6c01a00`                                             |
| Changed | `Local dependency-gated acceptance checks`           | `5d880a6`                                                                   |
| Fixed   | `Runtime readiness now classifies`                   | `884c2f9`, `87e2bb3`                                                        |
| Fixed   | `Claude invalid-effort rejection`                    | `228596a`                                                                   |
| Fixed   | `Codex legacy tool-result aliases`                   | `fd13b84`, `d78b060`, `20d46a7`, `c6fb134`                                  |
| Fixed   | `Gemini caller abort`                                | `953e0c2`                                                                   |
| Fixed   | `Kimi now applies caller-abort-first`                | `66031a4`, `6a8ae41`, `2a878dc`, `d5d5e65`                                  |
| Fixed   | `OpenCode caller abort`                              | `fb82691`, `c8f11bf`                                                        |
| Fixed   | `OpenCode permission correlation`                    | `1c5e631`, `9a9d516`, `ebae70d`, `206f322`                                  |
| Fixed   | `OpenCode managed startup`                           | `8347121`, `a0d7deb`                                                        |
| Fixed   | `tmux-play captured whole-message framing`           | `6e5d37e`                                                                   |
| Fixed   | `tmux-play observer delivery`                        | `f02b008`, `ef12492`                                                        |
| Fixed   | `tmux-play copy-mode follow`                         | `185fe2d`                                                                   |
| Fixed   | `tmux-play fanout acceptance`                        | `1b40579`                                                                   |

The table accounts for all 26 `fix` commits, both `test` commits, and the notable outcomes of the 115 documentation commits.
The remaining documentation commits are planning, migration mechanics, item-law splits, review corrections, citation retargets, or verification-debt bookkeeping whose externally relevant result is the single canonical-specification entry above.

## Pre-tag checklist

- [x] All unit tests pass — `npm test`: 48 files and 1,259 tests passed in the final release-preparation verification.
- [x] `npm run smoke:release` passes locally: build, package, distributable tarball / consumer / conformance audits, and 13 built-CLI smoke tests passed in order.
- [x] `CHANGELOG.md` contains `0.23.0`, dated `2026-08-29`, with the audited notable changes and updated links.
- [x] `package.json` carries version `0.23.0`.
- [x] The release changes and this evidence are prepared for one release-preparation commit; the containing commit satisfies the committed half of the gate.
- [ ] Push the release-preparation commit to `main` and require its CI run to pass — deliberately deferred.
- [ ] Create and push tag `v0.23.0` — explicitly outside this phase.

At the audited head, the unchanged product inputs had already passed typecheck, lint, build, 1,256 unit tests, package and distributable audits, 13 smoke checks, and every feasible acceptance flow (46 passed; 17 credential-dependent checks reported their skips).
Only checks whose inputs change in this preparation commit are rerun below before commit creation.

The final unit and release-smoke commands used temporary `PATH` shims for host prerequisites: the Kimi shim reported the repository's tested `0.31.1` version and delegated every non-version command to the installed Kimi binary, while the otherwise absent OpenCode shim exposed only the pinned `1.18.13` version and required `serve --help` flags.
The first unmodified-host unit run proved the only failures came from installed Kimi `0.27.0` being below the supported floor; the first release-smoke run stopped at the absent OpenCode executable.
