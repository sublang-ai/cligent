<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Release Preparation Evidence: 0.24.0

## Audit scope

- Previous tag: `v0.23.0`
- Audited head: `ab1e02a6fde63f3e5e4c889a08977bb431adf1e8`
- Commit count: `11`
- Ordered log SHA-256: `5187fa9574da971a2d3efe28e4f07a1380aaefca41ad67d6cfc73493bc4ceddf`
- Subject classes: `2 chore; 2 docs; 1 feat; 6 fix`
- Review command: `git log v0.23.0..ab1e02a6fde63f3e5e4c889a08977bb431adf1e8`
- Digest command: `git log --reverse --format='%H%x09%s' v0.23.0..ab1e02a6fde63f3e5e4c889a08977bb431adf1e8 | shasum -a 256`
- Review attestation: `complete`
- Containing changes attestation: `complete`

Every one of the eleven commits was reviewed from its message and diff.
The three runtime-target commits were reconciled to their final tested versions, compatibility floors, and repair policy; the five fast-mode commits were reconciled to the final DR-021 contract and implementation.
The three release-layer commits were reviewed for version metadata, shipped documentation, installed-package coverage, and release-audit behavior.
The staged contents and planned message of the symbolic containing release-preparation commit were also reviewed; its sole parent is the audited head, and it is the intended `v0.24.0` tag target.

## Semantic Versioning classification

- Previous version: `0.23.0`
- Chosen version: `0.24.0`
- Release date: `2026-08-31`
- Change level: `minor`

The range adds the public `fastMode` option, capability metadata and helpers, authentic observation payloads, and tmux-play configuration keys, so it is a new feature and requires a MINOR increment under release-1.
The API additions are optional or defaulted, omission preserves existing behavior, and unsupported adapters reject only a newly supplied option.
The runtime refresh raises no compatibility floor; Gemini's floor moves downward from 0.50.0 to 0.45.1, while every other floor remains unchanged.
The three audited release-layer commits and the containing preparation changes add version metadata, documentation, and release workflow, specification, and verification coverage without changing runtime behavior or compatibility.
The audited diff therefore confirms DR-021's additive, nonbreaking intent and contains no breaking change that would require a MAJOR release.

## Notable-change reconciliation

The changelog key is a unique phrase in the corresponding `0.24.0` entry.

| Heading | Changelog key                        | Commit evidence                                                          |
| ------- | ------------------------------------ | ------------------------------------------------------------------------ |
| Added   | `Adapter-scoped fast mode`           | `0bfd1bc`, `d78c506`, `d71c53c`, `957ad6e`, `c23bcb8`, `35e9464`; DR-021 |
| Changed | `Built-in agent runtime targets`     | `c6f891d`, `47ee472`, `c44a004`; DR-013                                  |
| Fixed   | `Final release-preparation auditing` | `35e9464`, `ab1e02a`; containing release-preparation commit; DR-020      |

The table accounts for all notable change groups in the audited range and containing preparation changes.
Commit `2f1fc0e` contains release metadata, evidence, changelog, and version alignment only, so it is not independently user-facing.
Commit `35e9464` completes the shipped fast-mode documentation and installed-package coverage in the Added row; its release-policy maintenance is consolidated into the Fixed row.
Commit `ab1e02a` establishes the sole-parent release boundary and distinct tagged-changelog diagnostics in the Fixed row.
The containing release-preparation commit scopes exact boundary enforcement to tag publication, keeps untagged CI usable, and removes the unreachable subject-class alias in the Fixed row; its evidence refresh and checklist mechanics are not independently notable.
Together the table and explicit dispositions account for every one of the eleven audited commits plus the containing preparation changes.

## Pre-tag checklist

- [x] All unit tests pass — `npm test`: passed in the final release-preparation verification, including the release-15 repository audit.
- [x] `npm run smoke:release` passes locally: build, package, distributable, and built-CLI smoke gates passed in order.
- [x] `CHANGELOG.md` contains `0.24.0`, dated `2026-08-31`, with the audited notable changes and updated links.
- [x] `package.json` carries version `0.24.0`; `package-lock.json` carries matching release metadata.
- [x] All release changes through the audited head are committed; the containing final release-preparation commit includes this refreshed evidence and the reviewed preparation changes, satisfying the committed half of the gate.
- [ ] Push the release-preparation commit to `main` and require its CI run to pass — deliberately deferred.
- [ ] Create and push tag `v0.24.0` — explicitly outside this phase.

The final release-preparation verification also passes `npm run typecheck` and `npm run lint`.
The containing final release-preparation commit changes only release evidence, changelog, release workflow and specification, and repository audit coverage; it changes no runtime implementation, runtime target, compatibility floor, selected version, or dependency.
