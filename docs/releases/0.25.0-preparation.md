<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Release Preparation Evidence: 0.25.0

## Audit scope

- Previous tag: `v0.24.0`
- Audited head: `0e592e6267e7df63bd2dd52b55909b44f2c0ac5b`
- Commit count: `9`
- Ordered log SHA-256: `920504c4526bbad9f7992be4689b10433a1898ec6f5df9f64e93c4a321e41ff3`
- Subject classes: `1 chore; 4 docs; 2 feat; 1 fix; 1 test`
- Review command: `git log v0.24.0..0e592e6267e7df63bd2dd52b55909b44f2c0ac5b`
- Digest command: `git log --reverse --format='%H%x09%s' v0.24.0..0e592e6267e7df63bd2dd52b55909b44f2c0ac5b | shasum -a 256`
- Review attestation: `complete`
- Containing changes attestation: `complete`

Every commit in the nine-commit range was considered from its message and changes.
The four documentation commits establish, approve and clarify DR-022; the two feature commits implement its adapter proof and host results; the fix commit repairs release verification and records completion. The earlier preparation and the safe-write acceptance correction were also reviewed.
The staged contents and planned message of the containing release-preparation commit were reviewed; its sole parent is the audited head and it is the intended `v0.25.0` tag target.

## Semantic Versioning classification

- Previous version: `0.24.0`
- Chosen version: `0.25.0`
- Release date: `2026-09-05`
- Change level: `minor`

The release adds optional typed host result data and a proven pre-execution rejection classification, requiring a MINOR increment.
Existing successful calls and unproven failures retain their behavior; no caller must supply a new member and no runtime compatibility floor changes.
The lockfile updates affect development dependencies; release metadata and the isolated Git fixture do not change the public API.

## Notable-change reconciliation

| Heading | Changelog key | Commit evidence |
| --- | --- | --- |
| Added | `SESSION_RESUME_REJECTED` | `b171114`, `d47565c`, `5992fe2`, `0930341`, `2b9923e`, `d048c9a`; DR-022 |
| Security | `fast-uri` | `5523553` |

The table accounts for every notable change group.
The four documentation commits are supporting decisions and clarifications for the Added group, with no separate behavior beyond that feature.
The licensing-fixture isolation, IR completion and verification notes in `5523553` are not independently user-facing; its dependency updates establish the Security group.
The earlier preparation `4b59b22` changes only release metadata and evidence; `0e592e6` removes a redundant shell check from the acceptance fixture while preserving host-side byte verification. Neither changes product behavior or warrants a separate changelog entry.
The containing release-preparation commit updates only this evidence record and is not independently notable.
Together these dispositions cover all nine audited commits and the containing preparation changes.

## Pre-tag checklist

- [x] All unit tests pass — `npm test`: 1,323 tests passed on the final implementation and npm dependency graph; release metadata is verified separately before committing.
- [x] `npm run smoke:release` passes locally: clean build, package output, distributable audits and 13 built-CLI smoke tests pass in order with isolated exact Gemini 0.57.0, Kimi 0.39.1 and OpenCode 1.18.25 CLIs.
- [x] `CHANGELOG.md` contains `0.25.0`, dated `2026-09-05`, with the audited changes and updated links.
- [x] `package.json` and both release-version fields in `package-lock.json` carry `0.25.0`.
- [x] All release changes through the audited head are committed; the containing release-preparation commit includes the reviewed metadata and this evidence.
- [ ] Push the release-preparation commit to `main` and require its CI run to pass — delegated to the release coordinator.
- [ ] Create and push tag `v0.25.0` — delegated to the release coordinator.

Typecheck, ESLint, spec lint and SPDX audit passed for the implementation.
The corrected live safe-write fixture passes Claude, Gemini 0.57.0, Kimi 0.39.1 and OpenCode 1.18.25. Codex created and updated the exact file bytes, but its local user-config stamp changed during the run; the cause is unproven, and the assertion remains mandatory in CI.
CI on the earlier preparation failed only the now-corrected Gemini command-substitution case; it does not authorize publication of this replacement.
The implementation, dependencies, version, package output and smoke inputs remain unchanged, so their passing results are retained. Only the corrected acceptance fixture, its ESLint check and this preparation's release audit were rerun; final CI remains pending.
